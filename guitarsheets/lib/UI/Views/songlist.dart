import 'package:flutter/material.dart';

class SongList extends StatelessWidget {
  @override 
  Widget build(BuildContext build) {
    return Scaffold (
      body: Text('Here is where the song list should be...'),
    );
  }
}